import React from "react";
import { BotonAdmin } from "../Button/BotonAdmin";
import { useNavigate } from "react-router-dom";

const SectionPay = () => {
  const navigate = useNavigate();

  return (
    <div classNameName=" w-9/10 min-h-[80vh] h-auto m-auto grid items-center justify-center sm:py-8"></div>
  );
};

export { SectionPay };
